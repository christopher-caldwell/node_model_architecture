# Memory Dump 10: Focused Tests

## What Changed

Added test script and test dependency:

- root `package.json`
  - `test`: `vitest run`
  - `vitest` dev dependency

Added tests:

- `packages/domain/test/domain.test.ts`
- `packages/application/test/lending-commands.test.ts`
- `scripts/check-boundaries.test.ts`

## Test Coverage

Domain tests cover:

- valid book copy transitions
- invalid book copy transitions
- valid member transitions
- invalid member transitions
- member loan-limit guard
- returned loan guard

Application tests cover:

- `LendingCommands.checkOutBookCopy` successfully creates a loan and commits the UoW
- checkout rolls back and throws `BookCopyCannotBeBorrowedError` when an active loan already exists for the copy

Boundary test covers:

- current workspace passes `scripts/check-boundaries.mjs`

## Verification

Ran:

- `pnpm install`
- `pnpm test`
- `pnpm typecheck`
- `pnpm lint:boundaries`

Results:

- 3 test files passed
- 8 tests passed
- full TypeScript build passed
- import boundary checker passed

Warnings from install:

- `@apollo/server@4.13.0` is deprecated
- pnpm ignored build scripts for `esbuild`

Tests still ran successfully despite the ignored `esbuild` script.

## Next Task Context

Task 11 is final verification and implementation notes.

Need run:

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint:boundaries`
- inspect `git status`
- validate `todo.json`

Then mark task 11 done and write final memory dump.
