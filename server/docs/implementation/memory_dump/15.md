# Task 15 Memory Dump: Replace Bootstrap Random Ident Generation With Nanoid

Status: done

User request:

- Replace the hand-rolled `randomBytes(8).toString("base64url").slice(0, 10)` identifier generation in `packages/server-bootstrap/src/deps.ts` with `nanoid`.

Changes made:

- Updated `packages/server-bootstrap/src/deps.ts`.
- Removed the `node:crypto` import.
- Added `import { nanoid } from "nanoid";`.
- Kept the existing `RandomIdentGenerator implements IdentGenerator` adapter and changed `generate()` to return `nanoid(10)`.
- Added `nanoid` as a dependency of `@library/server-bootstrap` via `pnpm --filter @library/server-bootstrap add nanoid`.
- `packages/server-bootstrap/package.json` now includes `"nanoid": "^5.1.9"`.
- `pnpm-lock.yaml` was updated by pnpm.

Architecture notes:

- The application layer still depends only on the `IdentGenerator` port.
- `nanoid` is isolated to the composition/bootstrap adapter package, so no domain or application boundary changes were introduced.
- Identifier length remains 10 characters, matching the previous adapter's public behavior.

Verification:

- Ran `pnpm --filter @library/server-bootstrap typecheck` from `servers/node`.
- Typecheck passed.

Next context:

- Task 16 is next: write a plan only, in `packages/persistence/docs/plans/pg_typed.md`, for replacing hand-written SQL strings/manual query generics with `pg-typed`.
- Do not implement `pg-typed` in task 16.
