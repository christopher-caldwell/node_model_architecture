# Memory Dump 5: Auth Core

## What Changed

Implemented `@library/auth-core`:

- `src/claims.ts`
- `src/errors.ts`
- `src/verifier.ts`
- `src/jwt.ts`
- `src/index.ts`

Added dependencies:

- `jsonwebtoken`
- `@types/jsonwebtoken`

## Auth Design

Exports:

- `Claims`
- `InvalidTokenError`
- `AuthVerifier`
- `JwtAuthAdapter`
- `jwtAudience`

The JWT adapter preserves the Rust behavior:

- HS256 only
- audience: `ops.craftcode.solutions`
- invalid tokens map to `InvalidTokenError`

The package imports no workspace packages. It is independent from domain/application business logic.

## Verification

Ran:

- `pnpm install`
- `pnpm --filter @library/auth-core typecheck`
- `pnpm lint:boundaries`

All passed.

## Next Task Context

Task 6 is PostgreSQL persistence.

Use `pg` for idiomatic Node Postgres access. Preserve these architecture facts:

- read repositories use a read-only pool
- write repositories are scoped to one transaction client
- `SqlWriteUnitOfWorkFactory` starts a transaction and provides write repos
- `commit()` commits once
- `rollback()` rolls back once
- write lookups use `FOR UPDATE` SQL
- read/write SQL remains separate, matching Rust intent

Persistence may import `@library/domain` and `@library/application`, but no transports.
