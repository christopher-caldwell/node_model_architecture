# Task 16 Memory Dump: Plan PgTyped Migration For Persistence SQL

Status: done

User request:

- Make a plan to replace the hand-done SQL with PgTyped.
- Write the plan in `packages/persistence/docs/plans/pg_typed.md`.
- Do not implement the PgTyped migration.

Research/context gathered:

- Current SQL is centralized in `packages/persistence/src/sql.ts`.
- Repository classes in `book.ts`, `book-copy.ts`, `member.ts`, and `loan.ts` call `pool.query<T>` or `client.query<T>` with manual generics.
- `mappers.ts` owns manual row interfaces and maps rows into domain entities.
- `uow.ts` owns transaction orchestration with `BEGIN`, `COMMIT`, and `ROLLBACK`; this should remain hand-written.
- Checked current PgTyped docs:
  - `https://pgtyped.dev/`
  - `https://github.com/adelsz/pgtyped`
- Relevant PgTyped behavior: SQL files can contain named queries, PgTyped generates parameter/result TypeScript types against a live PostgreSQL schema, and generated prepared queries run through a pg client/pool without string interpolation.

Changes made:

- Populated `packages/persistence/docs/plans/pg_typed.md`.
- The plan is documentation only. It does not add dependencies, generated files, SQL files, config, scripts, or repository refactors.

Plan summary:

- Add `@pgtyped/cli` and `@pgtyped/runtime` only when implementing the migration later.
- Add a package-local PgTyped config.
- Split `src/sql.ts` into `src/queries/book.sql`, `book-copy.sql`, `member.sql`, and `loan.sql`.
- Convert `$1`-style positional params to named PgTyped params while preserving Rust/DB data field names.
- Generate query modules such as `book.queries.ts`.
- Refactor repositories aggregate-by-aggregate, with Book first and Loan last.
- Keep generated types private to `@library/persistence`.
- Preserve domain/application/transport boundaries.

Verification:

- Ran `pnpm typecheck` from `servers/node`: passed.
- Ran `pnpm lint:boundaries` from `servers/node`: passed.
- Ran `pnpm test` from `servers/node`: passed, 3 files and 8 tests.

Next context:

- `packages/persistence/docs/plans/pg_typed.md` is ready as the migration guide.
- If the PgTyped migration is requested later, start by adding a new checkpoint task, then implement the plan incrementally.
